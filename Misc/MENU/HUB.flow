import( "HUB.msg" );
import("PERSON.flow")

void MessageTest() {
	
	MSG_WND_DSP();
	MSG( TestMsg, 0 );
	int option = SEL(TestSelect);
	MSG_WND_CLS();
	
	if (option == 0) {
	}
	else if (option == 1) {	
	}
	else if (option == 2) {		
	}
}